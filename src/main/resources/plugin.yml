name: MinecraftX
version: '1.0-SNAPSHOT'
main: paper.plugin.minecraftX.MinecraftX
api-version: '1.21'
commands:
  script:
    description: 运行或列出 MinecraftX Lua 脚本
    usage: /<command> run <name> | /<command> list
  mpm:
    description: 模块包管理
    usage: /<command> <install|remove|uninstall|archive|pack|packs|list|enabled|disabled> [args...]
